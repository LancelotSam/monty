#include "monty.h"
/**
 * _nop-this is the main function
 * it does nothing
 * @head:head of the stack
 * @idx: the counter or current index
 * Return:void
 */
void _nop(stack_s **head, unsigned int idx)
{
	(void) counter;
	(void) head;
}
